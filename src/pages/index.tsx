import type { NextPage } from "next";
import LoginForm from "../components/login-form";

const Home: NextPage = () => {
  return (
    <main>
      <LoginForm />
    </main>
  );
};

export default Home;
